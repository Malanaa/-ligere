setting up endpoints to recieve post
setting up mongo connection inputs
Bundling it into an open-source cli tool


-->configure the enpoints properly -> create a python scripts to interact whih those enpoitns, include authorization.